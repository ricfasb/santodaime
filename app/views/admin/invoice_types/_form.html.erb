<%= form_for(admin_invoice_type) do |f| %>
  <% if admin_invoice_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_invoice_type.errors.count, "error") %> prohibited this admin_invoice_type from being saved:</h2>

      <ul>
      <% admin_invoice_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
